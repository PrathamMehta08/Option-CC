# Groq API key for the AI assistant. Get one free at https://console.groq.com/keys
#
# OPTIONAL. Without it the screener, filters, sorting and charts all work; only the
# chat assistant is disabled, and it says so rather than failing obscurely.
GROQ_API_KEY=
